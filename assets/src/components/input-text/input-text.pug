mixin input-text(props)
    .input-text&attributes(attributes)
        input.form-control.input-text__input(type= props.type id= props.id placeholder= props.placeholder)
        if(props.smallText)
            small.form-text.text-muted= props.smallText
